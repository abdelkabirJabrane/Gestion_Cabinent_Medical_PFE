apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: medicab
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: auth-service
          image: abdojab/auth-service:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: "http://discovery-service:8761/eureka"
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres:5432/authdb"
            - name: SPRING_DATASOURCE_USERNAME
              value: "medicab"
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: medicab-secrets
                  key: postgres-password
            - name: SPRING_CLOUD_CONFIG_URI
              value: "http://config-service:8888"
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: medicab
spec:
  ports:
    - port: 8080
  selector:
    app: auth-service
