apiVersion: apps/v1
kind: Deployment
metadata:
  name: appointment-service
  namespace: medicab
spec:
  replicas: 1
  selector:
    matchLabels:
      app: appointment-service
  template:
    metadata:
      labels:
        app: appointment-service
    spec:
      imagePullSecrets:
        - name: regcred
      containers:
        - name: appointment-service
          image: abdojab/appointment-service:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: "http://discovery-service:8761/eureka"
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres:5432/appointmentdb"
            - name: SPRING_DATASOURCE_USERNAME
              value: "medicab"
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: medicab-secrets
                  key: postgres-password
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: appointment-service
  namespace: medicab
spec:
  ports:
    - port: 8080
  selector:
    app: appointment-service
